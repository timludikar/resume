@import "jeet"
@import "rupture"

#experience
  article
    aside
      cf()
      h3
        span(1/3)
      p
        text-align: right;
    div
      h4
        margin-top: 0;
    +above(rupture.mobile-cutoff)
      aside
        column(1/3)
        h3
          span(1/1)
        p
          text-align: left;
          text-indent: 0;
      div
        column(2/3)
